/** Optional detail controls for `read.getLinkDetail`. */
export interface LinkDetailReadOptions {
  /** Cap on recent events returned in `recentEvents` (default 5). */
  recentEventsLimit?: number;
}
